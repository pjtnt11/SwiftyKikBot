language: swift
os: osx
osx_image: xcode8.3
xcode_project: KikBot.xcodeproj
xcode_scheme: KikBot
before_install:
  - brew update
install:
  - swift package fetch
script:
  - swift test
# - travis_retry xcodebuild -project KikBot.xcodeproj -scheme KikBot test | xcpretty
after_success:
  - bash <(curl -s https://codecov.io/bash)
